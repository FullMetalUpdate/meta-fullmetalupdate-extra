LCONF_VERSION = "7"

BBPATH = "${TOPDIR}"
BSPDIR := "${@os.path.abspath(os.path.dirname(d.getVar('FILE', True)) + '/../..')}"

BBFILES ?= ""
BASELAYERS ?= " \
	${OEROOT}/sources/poky/meta \
	${OEROOT}/sources/poky/meta-poky \
	${OEROOT}/sources/poky/meta-yocto-bsp \
	${OEROOT}/sources/meta-fullmetalupdate \
	${OEROOT}/sources/meta-fullmetalupdate-extra \
	${OEROOT}/sources/meta-updater \
	${OEROOT}/sources/meta-machinelearning \
	${OEROOT}/sources/meta-st/meta-st-stm32mp \
	${OEROOT}/sources/meta-openembedded/meta-oe \
	${OEROOT}/sources/meta-openembedded/meta-python \
	${OEROOT}/sources/meta-openembedded/meta-filesystems \
	${OEROOT}/sources/meta-openembedded/meta-networking \
